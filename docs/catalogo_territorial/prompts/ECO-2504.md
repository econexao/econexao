# Prompt — ECO-2504: schema e contratos de dados

```text
/goal Implementar o schema aceito para tipos, proveniência, referências e selo SEMTUR sem duplicar atores.

Execute somente ECO-2504 após H25.1/H25.2. Siga o protocolo L: planejador →
implementador → testador → revisor → consolidador, sequenciais.

Leia documentação/changelog Supabase atuais antes de qualquer feature. Descubra a
CLI com supabase --help e crie migration oficialmente. Atualize migrations, modelos,
fixtures, contratos de dados e testes. Prefira derivar `is_semtur_inventory` de
referência institucional válida ou projeção segura, sem coluna manual divergente.

Cubra actor_types/aliases aplicáveis, constraints, índices, grants, RLS, views
security_invoker, testes positivos/negativos, migration list e advisors. Não importe
o snapshot, não altere produção e não inicie ECO-2505.
```
