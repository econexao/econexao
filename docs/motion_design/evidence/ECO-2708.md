# Evidências — ECO-2708 — Reconciliação e preparação de release

- **Base origin/staging:** `2d62f2724bc3f180e3e98638ad353f9dd94c3e32`
- **Branch da task:** `codex/eco-2708-motion`
- **Branch de integração:** `codex/motion-integration`
- **Ambiente:** Worktree dedicada isolada `C:\Users\Bruno\Downloads\eco-nexao-motion-2708`

---

## Resultados por Aceite

| Aceite | Resultado | Evidência / Descrição |
|---|---|---|
| **A1** | PASS | Staging head (`2d62f27`), branch de integração e candidato comparados. Diff de 58 arquivos estritamente restrito a `econexao-app/` e documentação em `docs/`. Sem backend, migrations, landing de produção ou segredos. |
| **A2** | PASS | Suíte completa de qualidade executada localmente (`npm ci`, `npm run typecheck`, `npm run openapi:check`, `npm test -- --watch=false`, `npm run export:web`, `npm run export:web:fixture`, `npm run test:browser` com 40/40 testes aprovados em desktop e mobile). |
| **A3** | PASS | Identidade remota do staging comprovada no projeto Vercel `econexao-app-staging` (equipe `eco-nexao`, alias canônico `https://econexao-app-staging.vercel.app/`, deployment ativo `6507502161`). Build normal web sem fixture concluído com sucesso. |
| **A4** | PASS | Procedimento de rollback documentado e executável: reversão do commit de squash em staging e/ou reatribuição de alias na Vercel para deployment anterior `6507502161`. Nenhuma alteração no backend/banco de dados. |
| **A5** | PASS | Pipeline GitHub Actions (`.github/workflows/staging-deploy.yml`) configurado com `APPLY_STAGING_MIGRATIONS=false`, garantindo bloqueio automático do deploy se houver avanço inesperado ou migration pendente. |
| **A6** | PASS | Plano de verificação de SHA via metadados de deployment e alias definido, assegurando distinção técnica entre o identificador de requisição (`x-vercel-id`) e o SHA de commit Git. |
| **A7** | PASS | Procedimento para a próxima task (ECO-2709 — Publicação e homologação) e autorizações operacionais mapeadas sem antecipar merge não autorizado. |

---

## Comandos Executados e Saídas

Todos os comandos foram executados no diretório `econexao-app` da worktree `C:\Users\Bruno\Downloads\eco-nexao-motion-2708`:

| Comando | Exit Code | Duração / Detalhes |
|---|---:|---|
| `npm ci` | 0 | 938 pacotes instalados/auditados |
| `npm run typecheck` | 0 | TypeScript check sem erros (`tsc --noEmit`) |
| `npm run openapi:check` | 0 | Sincronização OpenAPI confirmada |
| `npm test -- --watch=false` | 0 | 57 suítes, 354 testes aprovados (114.8s) |
| `npm run export:web` | 0 | Build web normal gerado em `dist/` (sem fixture) |
| `npm run export:web:fixture` | 0 | Build web com mocks contratuais para testes E2E |
| `npm run test:browser` | 0 | 40 testes Playwright aprovados (20 desktop + 20 mobile) |

---

## Artefatos e Manifesto de Release

- Manifesto detalhado registrado em [`release-candidate.md`](release-candidate.md).
- Status da integração revisado e pronto para abertura de PR `codex/motion-integration` → `staging`.

---

## Rollback

- Reversão local / PR de revert: branch `codex/revert-motion-integration` com `git revert -m 1 <SQUASH_SHA>`.
- Restauração de tráfego Vercel: reatribuição do alias para deployment anterior `6507502161` (`https://econexao-app-staging-hdv1nhnbr-eco-nexao.vercel.app`).


### 6. Publicação e PR #70
- **Pull Request**: [#70](https://github.com/econexao/econexao/pull/70)
- **Branch**: `codex/motion-integration`
- **Status Remoto**: Aberta, checks de CI verdes (`Validate branch promotion flow`, `contract`, `Vercel Preview`).
- **Garantia de Não-Interferência**: Não foi executado merge em staging, nem deploy de produção, nem alteração de branch protection remota.
