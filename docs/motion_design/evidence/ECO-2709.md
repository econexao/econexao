# ECO-2709 — Publicação e homologação em staging

## Resultado

**PARTIAL — PUBLICADA, NÃO HOMOLOGADA.** A iniciativa de motion foi publicada no
staging canônico, mas a homologação integral não pode receber o handoff
`PUBLICADO E HOMOLOGADO EM STAGING`: a jornada pública do detalhe de ator encontrou
HTTP 500 no endpoint de foto Google e os gates mobile, movimento reduzido e evidência
temporal de motion permaneceram `NOT_VERIFIABLE` nesta execução.

## Identidade do artefato

- Repositório: `https://github.com/econexao/econexao.git`
- PR: `#70`, squash merge autorizado pelo owner em 18/09/2026.
- Candidato revisado: `5b6acaf19ce601ba972fde2733ab43a6c5276c3c`.
- Base anterior de `staging`: `2d62f2724bc3f180e3e98638ad353f9dd94c3e32`.
- Squash publicado em `staging`: `943b11a426d91c87e83140f82e1e6c84ccf6de3b`.
- Workflow: `35341124936`, conclusão `success`.
- Deployment Vercel: `dpl_FnWnCm5ao4p44gmA352b6djW4Seg`, estado `READY`.
- URL imutável: `https://econexao-app-staging-696hs3eh4-eco-nexao.vercel.app`.
- Alias canônico confirmado no deployment: `https://econexao-app-staging.vercel.app/`.
- Backend staging: `https://econexao-backend-staging-30dt.onrender.com/` respondeu
  `commit_sha=943b11a426d91c87e83140f82e1e6c84ccf6de3b`.

O target Vercel é chamado `production` dentro do projeto dedicado
`econexao-app-staging`; nenhum projeto, alias ou ambiente de produção do produto foi
acessado.

## Aceites

| Aceite | Estado | Evidência |
|---|---|---|
| A1 — SHA esperado no alias e deployment READY | PASS | PR #70 gerou o squash `943b11a`; `vercel inspect` confirmou projeto `econexao-app-staging`, deployment READY e alias canônico. |
| A2 — pipeline do mesmo SHA e backend smoke | PASS | Workflow `35341124936`: backend, frontend, drift/advisors, secret scan e Render smoke concluíram com sucesso; backend expôs o SHA completo esperado. |
| A3 — jornadas públicas normal/reduce, sem novo erro | FAIL | Desktop público passou em carregamento, rota Pedral, mapa, pins, categorias, rota/cidade, quatro origens, traçado, catálogo e modal. O detalhe da Praia do Pedral recebeu HTTP 500 reproduzível em `/api/v1/actors/4ea01096-d60b-55ab-b66c-a47dbb917cd0/google-photo`. Mobile, reduce e motion temporal ficaram NOT_VERIFIABLE. |
| A4 — escritas de domínio | PASS COM LIMITE | Fluxos mutáveis permanecem comprovados somente pelas suítes locais anteriores. Nenhuma escrita remota foi autorizada ou executada nesta task. |
| A5 — sem production/schema/dados fora do escopo | PASS | Nenhuma migration, escrita de domínio, chamada Google Routes, mudança de ambiente ou acesso à landing de produção. |
| A6 — relatório liga PR, SHA, deployments e limitações | PASS | Este relatório registra identidades, comandos, incidente, riscos e rollback. |
| A7 — cadastro local distingue publicado de documental | PASS | `docs/project_status.md` atualizado localmente para `PARCIAL / PUBLICADA, NÃO HOMOLOGADA`; este registro não foi enviado a `staging`. |

## Evidência do browser real

Sessão BrowserAct única, pública e read-only, viewport desktop `1904x985`:

- home, lista de rotas, detalhe da Rota do Pedral, mapa expandido, catálogo e detalhe
  de ator carregaram;
- rota `a17a314a-0000-4000-8000-000000000002` exibiu Leaflet, traçado, pins e
  categorias reais;
- quatro origens disponíveis: Centro, Aeroporto, Rodoviária e Terminal Fluvial;
- modos `Ver rota` e `Ver cidade` alternaram com estado acessível;
- filtros observados: Atrativos 1, Transporte 39, Saúde 102 e Segurança 13;
- modal/preview da Praia do Pedral abriu e navegou ao catálogo/detalhe;
- CORS da API permitiu o alias canônico com credenciais;
- nenhuma mutation, login, criação de guest, GPS ou Google Routes foi acionada.

Incidente reproduzido também fora do browser:

```text
GET /api/v1/actors/4ea01096-d60b-55ab-b66c-a47dbb917cd0/google-photo
HTTP 500
code: INTERNAL_SERVER_ERROR
request_id (browser): req-6uhilfz4-1789732365087
request_id (reprodução CLI): req_688c4e0d5dd7426188a5c10195bcd164
```

A alteração publicada em `GooglePlacePhoto.tsx` limita-se ao estado visual de opacity
durante o carregamento; o diff da iniciativa não altera o backend. A documentação de
ECO-2613 já declarava Google real como não homologado. Portanto, não há evidência de
que o rollback do motion removeria o erro e ele não foi executado automaticamente.

## Comandos e resultados

| CWD | Comando | Resultado |
|---|---|---|
| `eco-nexao-motion-integration` | `gh pr view 70 --json ...` | PR aberta, CLEAN/MERGEABLE, head/base esperados e checks verdes. |
| `eco-nexao-motion-integration` | `gh pr merge 70 --squash` | exit 0; merge em 18/09/2026 11:44:34Z. |
| `eco-nexao-motion-integration` | `gh run view 35341124936 --json ...` | workflow completo `success`; todos os quatro jobs passaram. |
| `econexao-app` | `vercel inspect https://econexao-app-staging-696hs3eh4-eco-nexao.vercel.app --scope eco-nexao --json` | deployment READY, projeto e aliases confirmados. |
| `eco-nexao-motion-integration` | `Invoke-WebRequest https://econexao-backend-staging-30dt.onrender.com/` | HTTP 200; SHA esperado. |
| `eco-nexao-motion-2709` | GET público do endpoint `google-photo` | HTTP 500 reproduzível. |

Os gates locais completos do candidato permanecem registrados em `ECO-2708.md`:
typecheck, OpenAPI, Jest 57/354, export Web e Playwright 40/40. Não foram repetidos
após o squash porque o conteúdo publicado é o candidato já qualificado e o pipeline
do SHA final repetiu os gates contratuais relevantes.

## Incidente, próxima ação e rollback

Classificação: falha ativa de integração externa/mídia no backend staging, descoberta
durante a homologação. Próxima ação concreta: diagnosticar o request ID no Render,
corrigir o endpoint ou seu tratamento fail-closed em task própria/revisada, publicar
com GO e repetir A3 no alias canônico, incluindo desktop, mobile, movimento reduzido,
console/rede e vídeo/trace temporal.

Rollback disponível, mas não executado: criar branch de revert do squash `943b11a`,
abrir PR contra `staging` e, somente com autorização específica se necessário,
reatribuir temporariamente o alias ao deployment anterior `6507502161`. Como não há
mudança de backend no diff de motion e o erro Google real não foi previamente
homologado, esse rollback não possui evidência de que corrigiria o incidente.

## Handoff

`PARTIAL — PUBLICADA, NÃO HOMOLOGADA EM STAGING`.
