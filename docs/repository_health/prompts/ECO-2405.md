# Prompt — ECO-2405: legado técnico controlado

```text
/goal Remover ou arquivar somente legado tecnicamente provado, preservando dados e comportamento válidos.

Execute somente ECO-2405 após H24.2 e H24.3. Leia ADR 0013, deployment Google Routes,
contratos, configs, código, testes, CI e Git history. Orquestre arquiteto -> implementador
-> verificador. Use pesquisa primária local e, para Supabase/Google atuais, documentação
oficial quando uma decisão depender dela.

Avalie separadamente runtime/scripts/conector/testes OSRM, stats_summary.txt, cópia raiz
de elementos_interativos_telas.txt e all_migrations_consolidated.sql. Não presuma que
todos devem ser apagados. Para cada item, prove consumidores, finalidade, substituto,
regeneração e rollback. Preserve as três geometrias OSRM importadas e fixtures históricas
necessárias; FakeRoutingConnector de test/dev não é fallback de produção nem legado por si.

Faça alterações em grupos pequenos. Rode backend completo quando tocar Python, checks de
migration quando tocar Supabase e buscas regressivas por imports/configs. O verificador
reproduz testes e inspeciona se nenhum provider revogado permanece selecionável em runtime.

Aceite: zero mudança comportamental não intencional, zero dado removido, zero referência
quebrada e diff explica cada remoção. Se um consumidor não puder ser excluído, mantenha o
item e registre BLOCKED/PARTIAL. Não inicie ECO-2406.
```

